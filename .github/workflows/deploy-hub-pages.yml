name: Deploy hub Pages (deprecated)

# Replaced by CD — Hub Pages (cd-hub-pages.yml): build once, roll out on demand.
# Kept as a stub so old links fail clearly instead of auto-deploying the fleet.
on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::This workflow was replaced by CD — Hub Pages (cd-hub-pages.yml)."
          echo "Use Actions → CD — Hub Pages → Run workflow."
          exit 1

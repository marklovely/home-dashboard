{
  "name": "lovely-home-hub-api",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.19.0",
    "npm": ">=10.9.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:test": "node scripts/check-env-provisioned.mjs test && wrangler deploy --env test",
    "deploy:sandbox": "node scripts/check-env-provisioned.mjs sandbox && wrangler deploy --env sandbox",
    "provision:test": "bash ../scripts/provision-hub-site.sh test",
    "provision:sandbox": "bash ../scripts/provision-hub-site.sh sandbox",
    "d1:migrate:prod": "wrangler d1 migrations apply lovely-home-appliance-manuals --remote",
    "d1:migrate:test": "node scripts/check-env-provisioned.mjs test && wrangler d1 migrations apply lovely-home-appliance-manuals-test --remote --env test",
    "d1:migrate:sandbox": "node scripts/check-env-provisioned.mjs sandbox && wrangler d1 migrations apply lovely-home-appliance-manuals-sandbox --remote --env sandbox",
    "secrets:test": "bash scripts/print-env-secrets-checklist.sh test",
    "secrets:sandbox": "bash scripts/print-env-secrets-checklist.sh sandbox",
    "test": "vitest run",
    "check": "vitest run",
    "deploy:demo": "node scripts/check-env-provisioned.mjs demo && wrangler deploy --env demo",
    "d1:migrate:demo": "node scripts/check-env-provisioned.mjs demo && wrangler d1 migrations apply lovely-home-appliance-manuals-demo --remote --env demo",
    "secrets:demo": "bash scripts/print-env-secrets-checklist.sh demo",
    "deploy:dev": "node scripts/check-env-provisioned.mjs dev && wrangler deploy --env dev",
    "d1:migrate:dev": "node scripts/check-env-provisioned.mjs dev && wrangler d1 migrations apply lovely-home-appliance-manuals-dev --remote --env dev",
    "secrets:dev": "bash scripts/print-env-secrets-checklist.sh dev",
    "deploy:smith": "node scripts/check-env-provisioned.mjs smith && wrangler deploy --env smith",
    "d1:migrate:smith": "node scripts/check-env-provisioned.mjs smith && wrangler d1 migrations apply lovely-home-appliance-manuals-smith --remote --env smith",
    "d1:migrate:all": "node ../scripts/apply-d1-migrations-all.mjs",
    "deploy:all": "node ../scripts/deploy-all-workers.mjs",
    "secrets:smith": "bash scripts/print-env-secrets-checklist.sh smith",
    "deploy:wagtail": "node scripts/check-env-provisioned.mjs wagtail && wrangler deploy --env wagtail",
    "d1:migrate:wagtail": "node scripts/check-env-provisioned.mjs wagtail && wrangler d1 migrations apply lovely-home-appliance-manuals-wagtail --remote --env wagtail",
    "secrets:wagtail": "bash scripts/print-env-secrets-checklist.sh wagtail",
    "deploy:e2e-4xf0y4hx": "node scripts/check-env-provisioned.mjs e2e-4xf0y4hx && wrangler deploy --env e2e-4xf0y4hx",
    "d1:migrate:e2e-4xf0y4hx": "node scripts/check-env-provisioned.mjs e2e-4xf0y4hx && wrangler d1 migrations apply lovely-home-appliance-manuals-e2e-4xf0y4hx --remote --env e2e-4xf0y4hx",
    "secrets:e2e-4xf0y4hx": "bash scripts/print-env-secrets-checklist.sh e2e-4xf0y4hx"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.19.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "ical.js": "^2.2.1",
    "jose": "^5.10.0"
  }
}
